tasks.register('findLib') {
    doLast {
        configurations.implementation.findAll { it.name.contains('libausbc') }.each { file ->
            println "JAR FILE: $file"
            def zipFile = new java.util.zip.ZipFile(file)
            zipFile.entries().each { entry ->
                println entry.name
            }
        }
    }
}
